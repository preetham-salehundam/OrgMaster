// stream rows from the sheet named 'Your sheet name'
var excel = require('./index');
var fs = require('fs');

fs.createReadStream('bmw.xls')
.pipe(excel({
    sheet: 'Mejoras'
}))
.on('data', console.log);
